/* Match table prop templates — made in prop_lab.html.
   Each entry is one dressing of the table, drawn in array order.
   x / y / w are % of the match screen, rot is degrees,
   fx / fy are -1 when the prop is mirrored on that axis.
   Empty = the game falls back to procedural placement.

   Pure data: replace the whole file with a fresh "download prop_templates.js"
   from the lab whenever you like. Which template the game uses is set by
   FK_PROP_PIN in fark_proto.html, not here, so an export cannot clobber it. */
window.FK_PROP_TEMPLATES=[
];
