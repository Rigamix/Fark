# Playthrough notes 2026-08-15 — triaged

## PROGRESS (same day)
- **A1a DONE (P726/P726b, deployed)** — the "kept a 5" was the REST POSE:
  every rest-posed die on the match table showed a wrong face (_isoQ
  needed its lay variant under the high table camera); kept tray and
  pre-roll row were wrong too, silently. Plus: picker prefers the 1
  across all groups, and the seat is actually captured (foundLane was
  never assigned - P691 was stillborn).
- **A2 DONE (P727, deployed)** — machinery was proven working; the halo
  was hiding under Denis's thumb at phone scale. Armed now pops
  (scale 1.09) under a doubled halo; actives wear remaining-uses pips on
  the face (z-index:2 over the art, the .fcvTier lesson).
- **B DONE (P728, deployed)** — all nine, one probe drove them.
- **A3 DONE (P729, deployed)** — spinning-not-bouncing after rejoin was
  the LEGACY CSS ENGINE: backgrounding dropped the GL context, P551's
  handler called terminal _giveUp. Context loss now SUSPENDS (fail stays
  false); resumeMatch's existing boot() call revives on a fresh canvas.
  Verified with real WEBGL_lose_context loss.
- **A3b HALF-DONE** — the liked look IS the legacy D3 engine: face-ramp
  shading (0.55+0.55*dot), painted per-face shadows, no WebGL light rig.
  Recreating it deliberately in D3X = design work for the C15 lab.
- REMAINING: A4 (feat pin moment), C15 (VFX doc + lab), A1b (amber
  flow), C14 (bust scatter + red candle), A3b (adopt the look).

Fifteen notes, organized by priority then complexity. Screenshots referenced:
(1) PRESERVED announce overlapping dice row, (2) the liked "broken physics"
look, (3) HONEYTRAP tooltip overlapping score numbers.

## A — broken things, fix first

**A1. Preserve card is broken** — `HIGH complexity, TOP priority`
Preserved a 1, it kept a 5 (wrong die identity — suspect index-vs-value
mapping at the preserve site). Worse: the kept die is greyed out and
unrollable, so the player is a die short — actively harmful to pick the card.
Two halves:
- a) FUNCTIONAL FIX NOW: preserve the die that was tapped, and it must not
  cost a die.
- b) THE FLOW Denis specced (needs the VFX lab, C15): on use the die gets an
  amber overlay + audio; on bank it slides straight DOWN ITS OWN LANE
  (ambered, not greyed) out of the NPC dice's way; on the player's next roll
  it returns to its lane position only AFTER the thrown dice settle, scales
  up, amber shield dissolves. {mat,ench} travel with it, and it must survive
  save/reload mid-preserve (resume is a standing risk area).

**A2. Card armed-glow + spent grey-out still invisible** — `MEDIUM, third report`
Task #45 shipped a fix; Denis still sees neither the glow when dragging past
the threshold nor the grey-out after use. Classic reachability: the built
path vs the path he actually plays. Census every arming/spending route,
prove which one his drag takes, fix on THAT route. (feedback: test the
routes you didn't write; reachable before accurate.)

**A3. Physics broken after rejoining a match** — `MEDIUM, resume risk area`
Dice only spin, never bounce, after resume into a match (screenshot 2).
Find the resume-path difference (suspects: solver warm state, gravity/table
params not rehydrated, tape recorded against unscaled table). SEPARATE
deliverable A3b below for the look he liked in the same screenshot.

**A4. Feat won, no notification** — `MEDIUM`
Won teetotaller; entering the shelf gave nothing. Wanted: audio + the pin
landing on the board with glow + particles. Use the announce queue + existing
particle FX (use base mechanics); the pin moment itself is a C15 lab
candidate.

## B — one polish batch (nine small items, one pass, low risk)

1. NPC dialogue box a bit higher.
2. Bank-to-win: bottom buttons switch scale; after tapping BANK keep that
   scale, don't snap back.
3. Win screen: card SLOTS (not cards) down a few px.
4. Match tooltips: appear faster.
5. Match tooltips: body text in the dialogue font (title + "uses left" keep
   theirs).
6. Tooltip open → hide the floating score numbers (screenshot 3 overlap).
7. Card effect announce text ABOVE the dice row (screenshot 1 overlap).
8. Shelf focus: hide the dotted background slot outlines.
9. Shelf: hide top hearts + gold. / Store: enchant focus panel opens even
   when unaffordable, with the dice panel's "can't buy" button.

## C — VFX and design work

**C15. Card VFX ideation doc + effects lab** — `HIGH, enables A1b/A4/C14`
One doc: per-card visual effect, step by step, optimized for player clarity
and REUSE of the existing pipe (announce queue, D3X, keyframes, particle
sprays) — no new systems without a reason. Plus a standalone lab page
(fark_lab.html style, isolated like fark_nights.html) to preview each effect
on dummy dice/cards before it touches the game.

**C14. Bust scatter feels wrong** — `MEDIUM`
Dice part in the middle and bunch up instead of true chaos — the impulse
field looks centre-out; want varied directions/spins. Plus: try the candle
light shifting red/orange during the bust beat.

**A3b. The accidental look (screenshot 2)** — `MEDIUM, design exploration`
Liked: darker but MORE GRADUAL side shadows, less at the die's base (reads
as bounce light); selection glow sharper/thinner at base, softer at top
(vertical directional blur); darker environment overall. Analyze what state
produced it (likely the broken-resume render state), then recreate
deliberately. Denis's idea to explore: environment darkens progressively as
score approaches the target.

## Recommended execution order

A1a → A2 → B batch → A3 → A4 → C15 (doc+lab) → A1b → C14 → A3b.
Rationale: stop the harm first (A1a), close the thrice-reported gap (A2),
bank the nine cheap wins, then the investigations, then the lab unlocks the
three big visual moments.
